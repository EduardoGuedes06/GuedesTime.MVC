@model GuedesTime.MVC.ViewModels.DisciplinaViewModel

@{
    ViewData["Title"] = (Model.Id == null || Model.Id == Guid.Empty) ? "Cadastrar Disciplinas" : "Editar Disciplina";
}

<div class="card" style="margin-top: 0;">
    @await Html.PartialAsync("_Upsert", Model)
</div>

